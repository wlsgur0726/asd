#include "stdafx.h"
#include "test.h"
#include "asd/memdump.h"
#include <thread>

void Test()
{
	//for (int i=0; i<3; ++i) {
	//	asd::MemDump::Create();
	//	std::this_thread::sleep_for(std::chrono::seconds(1));
	//}
	//std::terminate();
}
